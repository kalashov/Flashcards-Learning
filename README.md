# Flashcards - Приложение для изучения с помощью флеш-карточек

Приложение для изучения с помощью флеш-карточек, состоящее из iOS приложения на SwiftUI и REST API на FastAPI.

## 📋 Описание

Flashcards - это интерактивное приложение для изучения материала с помощью флеш-карточек. Приложение позволяет пользователям просматривать карточки с вопросами и ответами, отмечать свой прогресс и отслеживать результаты обучения.

### Основные возможности

- 🎯 **Интерактивные карточки**: Просмотр вопросов и ответов с возможностью переворота карточки
- 📊 **Отслеживание прогресса**: Сохранение статистики по каждой карточке (знаю/не знаю)
- 👤 **Персонализация**: Вход по имени пользователя с сохранением результатов
- 📈 **Статистика**: Просмотр результатов после завершения сессии
- 🔄 **Повторное использование**: Возможность начать новую сессию обучения

## 🏗️ Архитектура проекта

Проект состоит из двух основных компонентов:

1. **iOS приложение** (`FLASHCARDS_NFACT_IOS/flashcards/`) - клиентское приложение на SwiftUI
2. **Backend API** (`FLASHCARDS_NFACT_IOS/flashcards_api/`) - REST API на FastAPI с PostgreSQL

## 🚀 Быстрый старт

### Предварительные требования

- Python 3.8+
- PostgreSQL (можно использовать Docker)
- Xcode 12+ (для iOS приложения)
- Docker (опционально, для упрощения запуска БД)

### Установка и запуск Backend API

1. **Перейдите в директорию API:**
```bash
cd FLASHCARDS_NFACT_IOS/flashcards_api
```

2. **Создайте виртуальное окружение (рекомендуется):**
```bash
python -m venv venv
source venv/bin/activate  # На Windows: venv\Scripts\activate
```

3. **Установите зависимости:**
```bash
pip install -r requirements.txt
```

4. **Настройте базу данных:**

   Создайте файл `.env` в директории `flashcards_api/`:
   ```env
   DATABASE_URL=postgresql://postgres:postgres@localhost:5433/flashcards
   ```

   Запустите PostgreSQL через Docker:
   ```bash
   docker run --name flashcards-db -e POSTGRES_PASSWORD=postgres -p 5433:5432 -d postgres
   ```

   Или используйте существующую базу данных PostgreSQL.

5. **Запустите сервер FastAPI:**
```bash
uvicorn app.main:app --reload
```

6. **Загрузите карточки:**

   - Откройте Swagger документацию: http://127.0.0.1:8000/docs
   - Найдите эндпоинт `POST /load_cards`
   - Нажмите "Try it out" → "Execute"
   - Карточки будут загружены из Open Trivia Database

### Запуск iOS приложения

1. **Откройте проект в Xcode:**
```bash
cd FLASHCARDS_NFACT_IOS/flashcards/flashcards/flashcards.xcodeproj
open flashcards.xcodeproj
```

2. **Настройте URL API (если необходимо):**

   В файле `FlashcardService.swift` измените `baseURL`, если ваш API работает на другом адресе:
   ```swift
   private let baseURL = "http://127.0.0.1:8000"
   ```

   **Важно:** Для работы на реальном устройстве iOS используйте IP-адрес вашего компьютера вместо `127.0.0.1`.

3. **Запустите приложение:**

   - Выберите симулятор или подключенное устройство
   - Нажмите Run (⌘R)

## 📱 Использование приложения

1. **Запуск:** При первом запуске введите ваше имя
2. **Изучение:** Просматривайте карточки, нажимайте "Flip" для просмотра ответа
3. **Оценка:** Нажимайте "Знаю" или "Не знаю" для каждой карточки
4. **Результаты:** После прохождения всех карточек просмотрите статистику
5. **Повтор:** Нажмите "Играть снова" для новой сессии

## 🔌 API Endpoints

### `GET /`
Проверка работоспособности API
- **Ответ:** `{"message": "Flashcards API is running"}`

### `GET /cards`
Получить все карточки
- **Ответ:** Список карточек в формате JSON

### `POST /load_cards`
Загрузить карточки из Open Trivia Database
- **Ответ:** `{"message": "Cards loaded successfully"}`

### `POST /progress`
Создать запись о прогрессе пользователя
- **Тело запроса:**
  ```json
  {
    "user_id": "string",
    "card_id": 1,
    "status": "know" | "dont_know"
  }
  ```

### `GET /progress?user_id={user_id}`
Получить прогресс пользователя
- **Параметры:** `user_id` (query parameter)
- **Ответ:** Список записей о прогрессе

## 📂 Структура проекта

```
flashcards-1/
├── FLASHCARDS_NFACT_IOS/
│   ├── flashcards/                    # iOS приложение
│   │   └── flashcards/
│   │       └── flashcards/
│   │           ├── ContentView.swift      # Главный view
│   │           ├── StartView.swift        # Экран входа
│   │           ├── FlashcardsView.swift   # Экран с карточками
│   │           ├── ResultView.swift       # Экран результатов
│   │           ├── FlashcardService.swift # Сервис для работы с API
│   │           ├── ProgressService.swift  # Сервис для отправки прогресса
│   │           └── UserDefaultsService.swift # Локальное хранилище
│   │
│   └── flashcards_api/               # Backend API
│       └── app/
│           ├── main.py                # FastAPI приложение
│           ├── models.py              # SQLAlchemy модели
│           ├── schemas.py             # Pydantic схемы
│           ├── database.py            # Настройка БД
│           └── utils.py               # Утилиты (загрузка карточек)
│       ├── requirements.txt           # Python зависимости
│       └── README.md                  # Документация API
└── README.md                          # Этот файл
```

## 🛠️ Технологии

### Backend
- **FastAPI** - современный веб-фреймворк для Python
- **SQLAlchemy** - ORM для работы с базой данных
- **PostgreSQL** - реляционная база данных
- **httpx** - асинхронный HTTP клиент
- **Pydantic** - валидация данных

### iOS
- **SwiftUI** - фреймворк для создания пользовательского интерфейса
- **Foundation** - работа с сетью и данными
- **UserDefaults** - локальное хранилище

## 📝 Модели данных

### Card (Карточка)
- `id` - уникальный идентификатор
- `question` - вопрос
- `answer` - ответ
- `topic` - тема/категория

### Progress (Прогресс)
- `id` - уникальный идентификатор
- `user_id` - идентификатор пользователя
- `card_id` - идентификатор карточки
- `status` - статус ("know" или "dont_know")

## 🔧 Настройка

### Изменение источника карточек

По умолчанию карточки загружаются из Open Trivia Database. Чтобы изменить источник, отредактируйте функцию `load_cards_from_opentdb` в файле `app/utils.py`.

### Настройка базы данных

Измените переменную `DATABASE_URL` в файле `.env` для подключения к другой базе данных.

### Настройка порта API

Измените команду запуска:
```bash
uvicorn app.main:app --reload --port 8000
```

## 🐛 Решение проблем

### iOS приложение не может подключиться к API

- Убедитесь, что API запущен и доступен
- Для симулятора используйте `127.0.0.1` или `localhost`
- Для реального устройства используйте IP-адрес вашего компьютера в локальной сети
- Проверьте, что порт 8000 не заблокирован файрволом

### Ошибка подключения к базе данных

- Убедитесь, что PostgreSQL запущен
- Проверьте правильность `DATABASE_URL` в файле `.env`
- Убедитесь, что порт 5433 (или другой указанный порт) доступен

### Карточки не загружаются

- Проверьте подключение к интернету (для загрузки из Open Trivia Database)
- Убедитесь, что эндпоинт `/load_cards` был вызван успешно
- Проверьте логи сервера на наличие ошибок

## 📄 Лицензия

Этот проект создан в образовательных целях.

## 👤 Автор

Ramazan Perdebai

## 🙏 Благодарности

- Open Trivia Database за предоставление API для получения вопросов
- FastAPI за отличный фреймворк
- SwiftUI за удобный инструмент разработки iOS приложений

